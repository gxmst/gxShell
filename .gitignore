build/bin
build/*.exe
build/windows/installer/tmp/
node_modules
frontend/dist
release/
*.zip
*.log
*.tmp
gxShell.exe
gxshell-cli.exe
*.bak
*.dat
.secretkey
todo.md
frontend/package.json.md5
.gxshell_key
*.pem
*.key
.env
.env.*
.codex/

# Vite build temp files
frontend/vite.config.ts.timestamp-*.mjs

# TypeScript incremental build artifacts
*.tsbuildinfo

# Ad-hoc scratch scripts and pasted screenshots. These accumulate at the repo
# root while debugging unrelated things and are never part of a commit. The
# leading slash keeps this to the root: icons and doc assets live in
# build/, docs/ and frontend/src/assets/ and must stay tracked.
/tmp_*
/*.png
/.staging/
/.tmp/
/.agents/
